# betterfarm-admin

> **Archived 2026-05-15** — this repository is read-only. Active development has moved into the BetterFarm monorepo.

## New location

- **Repository:** [`Aryaveer716/Betterfarm`](https://github.com/Aryaveer716/Betterfarm) (private)
- **Path in monorepo:** `apps/admin/`

Contact [@Aryaveer716](https://github.com/Aryaveer716) for access.

## What this was

The original standalone `@betterfarm/admin` package — the BetterFarm admin portal (TypeScript + Vite + Drizzle). Its final commit (2026-05-14) has been ported into the monorepo; the most recent fix on top of that — admin-chat OpenRouter error surfacing + Gemini fallback — lives at monorepo `main` commit `51f631c`.
